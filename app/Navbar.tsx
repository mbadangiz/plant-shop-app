import { Box, Container } from "@mui/material";

const Navbar = () => {
  return (
    <Container
      maxWidth="lg"
      sx={{
        bgcolor: "red",
        padding: { md: "40px 0  10px 0 !important" },
      }}
    >
      1
    </Container>
  );
};

export default Navbar;
